faster-whisper>=1.0
opencv-python-headless>=4.8
numpy>=1.24
# yt-dlp is vendored here (was previously curl'd from GitHub latest on every
# Docker build). Kept unpinned because YouTube site changes break older
# yt-dlp versions constantly — we want pip to fetch a fresh one each build,
# but the wheel lookup is cached, which is still faster than a GitHub download.
yt-dlp
# P3.5-C: lightgbm is only used by train_scorer.py and the optional
# supervised-scorer inference path. If the lib is missing, score.py silently
# falls back to the linear formula, so this is effectively a train-time dep.
lightgbm>=4.0
scikit-learn>=1.3
pytest>=8.0
pytest-cov>=5.0
